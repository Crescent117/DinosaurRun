
	export let canvas = document.getElementById("canvas");
	export let ctx = canvas.getContext("2d");

	export let canvasWidth = canvas.width = window.innerWidth - 100;
	export let canvasHeight = (canvas.height = window.innerHeight - 100);

